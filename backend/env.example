# Database Configuration
# Using your MongoDB Atlas connection string (password contains '@' and is URL-encoded as %40)
MONGODB_URI=mongodb+srv://ehsanhayatiwiz:Qwerty1122%40@cluster0.49i8d9j.mongodb.net/hrms_iwiz?retryWrites=true&w=majority&appName=Cluster0

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRE=7d

# Server Configuration
# PORT is provided by Render automatically
# Do not set PORT on Render; leave it unset
NODE_ENV=production

# CORS Configuration
# Set to your Vercel frontend URL (with protocol)
CORS_ORIGIN=https://hrms-iwiz-frontend.vercel.app

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=300

# Email Configuration (for future use)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password


# Failsafe Admin (auto-create/ensure admin on every boot)
# It uses plain password because Admin model comparePassword is plain-text currently
ADMIN_EMAIL=irtazamadadnaqvi@iwiz.com
ADMIN_PASSWORD=03145372506
ADMIN_NAME=Failsafe Admin

